// src/hooks.ts
import { createTRPCHandle } from 'trpc-sveltekit';
// import { AppRouter } from '../../src/trpc/trpc.router'
// create your tRPC router...

// export const handle = createTRPCHandle({ url: '/trpc', router: AppRouter });